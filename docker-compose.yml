services:
  ocr-recognizer:
    build: .
    ports:
      - "8001:8000"
    volumes:
      - ./tmp:/tmp/ocr-recognizer
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "2.0"
          memory: 2G
